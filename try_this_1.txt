20-minute challenge 

Load up 'tools.json' (copy from chat)

Write code to grab and show
- the entire 'tools' data structure
- just the 'name' then just the top-level 'description'
- the entire 'input_schema'
- just the 'data'
- the 'required' bit


